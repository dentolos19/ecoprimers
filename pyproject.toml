[project]
name = "ecoprimers"
version = "1.0.0"
requires-python = ">=3.12,<3.14"
dependencies = [
  "alembic",
  "authlib",
  "flask",
  "flask-socketio",
  "flask-sqlalchemy",
  "newsapi-python",
  "openai",
  "pandas",
  "pillow",
  "plotly",
  "psycopg[binary]",
  "python-dotenv",
  "requests",
  "resend",
  "sqlalchemy",
  "uvicorn",
  "xlsxwriter",
]

[dependency-groups]
dev = ["ty", "ruff"]

[tool.ruff]
line-length = 120
